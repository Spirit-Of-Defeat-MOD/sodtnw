#Category only allowed for GER
GER_labour_relations_category = {
	GER_labour_meeting_decision = {
		days_mission_timeout = 70
		selectable_mission = no

		available = {
			NOT = {
				has_country_flag = GER_labour_meeting_flag
			}
        	
    	}
		activation = {
            has_completed_focus = GER_emergency_labour_negotiations
        }
	}

	GER_send_invite_decision = {
		cost = 10
		fire_only_once = yes

		available = {
			has_country_flag = GER_labour_meeting_flag
    	}
		complete_effect = {
			custom_effect_tooltip = GER_invites_sent_tt
			
			hidden_effect = {
				country_event = { id = germany_labour.1 days = 1}
			}
		}
	}

	GER_release_detainee_decision = {
		cost = 15
		fire_only_once = yes
		days_remove = 10

		available = {
			has_country_flag = GER_labour_meeting_flag
			NOT = {
				has_country_flag = GER_not_trying_to_sway_spd_flag
			}
    	}
		complete_effect = {
			custom_effect_tooltip = GER_release_detainee_tt
			set_country_flag = GER_not_trying_to_sway_spd_flag
			
		}

		remove_effect = {
			clr_country_flag = GER_not_trying_to_sway_spd_flag
		}
	}

	GER_recruit_neutral_mediators_decision = {
		cost = 15
		fire_only_once = yes
		days_remove = 15

		available = {
			has_country_flag = GER_labour_meeting_flag
			NOT = {
				has_country_flag = GER_not_trying_to_sway_spd_flag
			}
    	}
		complete_effect = {
			custom_effect_tooltip = GER_recruit_neutral_mediators_tt
			set_country_flag = GER_not_trying_to_sway_spd_flag
			
		}

		remove_effect = {
			clr_country_flag = GER_not_trying_to_sway_spd_flag
		}
	}

	GER_withdraw_military_decision = {
		cost = 20
		fire_only_once = yes
		days_remove = 25

		available = {
			has_country_flag = GER_labour_meeting_flag
			NOT = {
				has_country_flag = GER_not_trying_to_sway_spd_flag
			}
    	}
		complete_effect = {
			custom_effect_tooltip = GER_withdraw_military_tt
			set_country_flag = GER_not_trying_to_sway_spd_flag
			
		}

		remove_effect = {
			clr_country_flag = GER_not_trying_to_sway_spd_flag
		}
	}

	GER_open_military_depots_decision = {
		cost = 15
		fire_only_once = yes
		days_remove = 20

		available = {
			has_country_flag = GER_labour_meeting_flag
			NOT = {
				has_country_flag = GER_not_trying_to_sway_spd_flag
			}
    	}
		complete_effect = {
			custom_effect_tooltip = GER_open_military_depots_tt
			set_country_flag = GER_not_trying_to_sway_spd_flag
			
		}

		remove_effect = {
			clr_country_flag = GER_not_trying_to_sway_spd_flag
		}
	}
}